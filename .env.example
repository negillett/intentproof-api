INTENTPROOF_ENV=dev
INTENTPROOF_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/intentproof_api
INTENTPROOF_API_KEYS={"dev-local-key":"tenant-dev"}

# Optional: after successful append-only ingest, notify verification workers (requires boto3 — declared in pyproject).
# INTENTPROOF_SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/intentproof-verification
# INTENTPROOF_AWS_REGION=us-east-1
